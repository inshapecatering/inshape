Claves de administrador admin@catering.local
admin123
Clave para cliente de prueba 98765432 y 78806923

git pull origin main
git add .
git commit -m "Descripción de los cambios"
git push origin main

git add -A
git commit -m "Sincronizando con carpeta local"
git push origin main --force

Antes de subir, abre sw.js y cambia la primera línea:
js
   const CACHE_NAME = 'catering-control-v1';