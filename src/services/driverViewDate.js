// Fusionado con workViewDate.js (misma lógica de storage, un solo archivo). Re-export para no
// romper el import de DispatchPage.jsx.
export { getDriverViewDate, setDriverViewDate } from './workViewDate';
